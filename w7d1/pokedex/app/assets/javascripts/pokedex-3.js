Pokedex.RootView.prototype.reassignToy = function (event) {

  var $li = $('<li>').text(toy.escape('name'))
  $ul.append($li);
  
};

Pokedex.RootView.prototype.renderToysList = function (toys) {

};
